# Keep the PEP 517 backend available for offline, non-isolated builds.
# This version must match [build-system].requires in pyproject.toml.
hatchling==1.32.0
# Hatchling requests this dynamically when building editable installations.
editables==0.5
